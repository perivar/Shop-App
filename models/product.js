class Product {
  constructor(id, userId, seller, name, url, description, price){
    this.id = id;
    this.userId = userId;
    this.seller = seller
    this.name = name;
    this.url = url;
    this.description = description;
    this.price = price;
  }
}

export default Product;
